const IMAGE_LARGE = {
  width: 300,
  height: 380,
  deviceScaleFactor: 1
};

const IMAGE_COMPACT = {
  width: 300,
  height: 80,
  deviceScaleFactor: 1
}

module.exports = {
  large: IMAGE_LARGE, // for bracket reference (eg. CONSTANTS['large'])
  compact: IMAGE_COMPACT
};